# Crazyflie 2.1 Motion planning in cluttered environment
This is an offboard control of the Crazyflie drone using neural contraction-based tracking controller. The client code is located in ```CCM_deploy.py ```. Qualisys motion capture is used for inertial state estimation, which can be disabled if not required.

To restart the client, run the ```reboot.py``` file. 
